version: '3.9'

services:
  cold-start:
    build:
      context: .
      dockerfile: ./apps/cold-start/Dockerfile.dev
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - /app/node_modules