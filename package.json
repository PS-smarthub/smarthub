{
  "name": "smarthub",
  "private": true,
  "scripts": {
    "create-app": "cd ./apps/ && smarthub-cli create-app",
    "start": "turbo start",
    "build": "turbo build",
    "build:cold-start": "turbo build -w=cold-start",
    "build:smarthub": "turbo build -w=smarthub",
    "dev": "dotenv -- turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "husky": "^9.0.11",
    "prettier": "^3.1.1",
    "turbo": "^2.0.9"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "npm@10.5.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "dotenv-cli": "^7.4.2",
    "smarthub": "^1.0.0",
    "smarthub-cli": "^0.6.0"
  }
}
